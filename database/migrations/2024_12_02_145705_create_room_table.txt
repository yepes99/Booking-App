<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('rooms', function (Blueprint $table) {
            $table->id(); // ID único de la habitación
            $table->foreignId('hotel_id')->constrained()->onDelete('cascade'); // Relación con el hotel
            $table->string('room_type'); // Tipo de habitación (ej. 'doble', 'suite', 'familiar', 'individual')
            $table->integer('capacity'); // Capacidad de personas
            $table->integer('floor_number')->nullable(); // Número de piso en el hotel
            $table->string('bed_type')->nullable(); // Tipo de cama (ej. 'king size', 'queen size', 'individual')
            $table->integer('number_of_beds')->default(1); // Número de camas en la habitación
            $table->boolean('has_wifi')->default(true); // Indica si la habitación tiene Wi-Fi
            $table->boolean('has_tv')->default(true); // Indica si la habitación tiene TV
            $table->boolean('has_air_conditioning')->default(true); // Indica si tiene aire acondicionado
            $table->boolean('has_heating')->default(true); // Indica si tiene calefacción
            $table->boolean('has_minibar')->default(false); // Indica si tiene minibar
            $table->boolean('has_safe_box')->default(false); // Indica si tiene caja fuerte
            $table->decimal('price_per_night', 8, 2); // Precio por noche
            $table->enum('status', ['available', 'booked', 'maintenance', 'closed'])->default('available'); // Estado de la habitación
            $table->text('description')->nullable(); // Descripción de la habitación
            $table->text('amenities')->nullable(); // Servicios adicionales de la habitación en formato JSON
            $table->boolean('is_smoking_allowed')->default(false); // Indica si se permite fumar
            $table->boolean('is_pet_friendly')->default(false); // Indica si es apta para mascotas
            $table->boolean('is_accessible')->default(false); // Indica si es accesible para personas con discapacidades
            $table->timestamps(); // Fechas de creación y actualización
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('rooms');
    }
};
